require "spec_helper"

describe PiLightsControl do
  it "has a version number" do
    expect(PiLightsControl::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
